# ai-things


```
    ./artisan job:GenerateWav 2
    ./artisan job:GenerateSrt 2
    ./artisan job:GenerateMp3 2
    ./artisan job:PromptForImage 2
    ./artisan job:GeneratePodcast 2
    ./artisan job:UploadPodcastToYoutube 2
```


## Install notes

```
yay -S sox libmad ffmpeg php php-pgsql php-gd redis php-redis python-pluggy python-pycosat python-ruamel-yaml  python-pluggy python-pycosat python-ruamel-yaml wget

pip install piper-tts
pip install pika python-dotenv torch torchaudio psycopg2 progressbar transformers datasets accelerate


pip install httplib2 apiclient

pip install google-auth-oauthlib google-api-python-client
```

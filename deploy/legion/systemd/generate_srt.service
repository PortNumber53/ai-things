[Unit]
Description=Go Manager Command for Generating SRT files
After=network.target

[Service]
Type=simple
User=grimlock
Group=grimlock
WorkingDirectory=/deploy/ai-things/current
ExecStart=/usr/bin/bash -c "source /home/grimlock/miniforge3/etc/profile.d/conda.sh && conda activate speech && /deploy/ai-things/current/manager-go/manager job:GenerateSrt --sleep=30 --verbose"
Restart=always
RestartSec=3
SyslogIdentifier=generate_srt

[Install]
WantedBy=multi-user.target

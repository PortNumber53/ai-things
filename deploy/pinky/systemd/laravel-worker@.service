[Unit]
Description=Laravel Queue Worker for %I
After=network.target

[Service]
User=grimlock
Group=grimlock
Restart=always
WorkingDirectory=/deploy/ai-things/current/api/
ExecStart=/usr/bin/php artisan queue:work --sleep=3 --tries=3 --queue=%I

[Install]
WantedBy=multi-user.target

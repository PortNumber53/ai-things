docker run -d \
  --restart=unless-stopped \
  --hostname zenrabbit \
  --name zenrabbit \
  -p 5672:5672 \
  -p 15672:15672 \
  -v /docker/zenrabbit/data:/var/lib/rabbitmq \
  --env-file _extra_env \
  rabbitmq:3-management
